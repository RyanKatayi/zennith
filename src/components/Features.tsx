import { Badge } from "./ui/badge";
import {
  Card,
  CardContent,
  CardFooter,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import image from "../assets/growth.png";
import image3 from "../assets/reflecting.png";
import image4 from "../assets/looking-ahead.png";

interface FeatureProps {
  title: string;
  description: string;
  image: string;
}

const features: FeatureProps[] = [
  {
    title: "Discover Scholarships",
    description:
      "Browse through a vast database of scholarships tailored to your profile and preferences.",
    image: image4,
  },
  {
    title: "AI-Powered SOP Generator",
    description:
      "Leverage our AI tools to create impressive Statements of Purpose tailored to each scholarship application.",
    image: image,
  },
  {
    title: "AI-Powered Resume Builder",
    description:
      "Use our AI-powered resume builder to create professional resumes that highlight your strengths and achievements.",
    image: image3,
  },
];

const featureList: string[] = [
  "Discover Scholarships",
  "Save Deadlines",
  "Get Application Help",
  "AI-Powered SOP Generator",
  "AI-Powered Resume Builder",
  "Monitor Progress",
  "Access Support",
];

export const Features = () => {
  return (
    <section
      id="features"
      className="container py-24 sm:py-32 space-y-8"
    >
      <h2 className="text-3xl lg:text-4xl font-bold md:text-center">
        Many{" "}
        <span className="bg-gradient-to-b from-primary/60 to-primary text-transparent bg-clip-text">
          Great Features
        </span>
      </h2>

      <div className="flex flex-wrap md:justify-center gap-4">
        {featureList.map((feature: string) => (
          <div key={feature}>
            <Badge
              variant="secondary"
              className="text-sm"
            >
              {feature}
            </Badge>
          </div>
        ))}
      </div>

      <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        {features.map(({ title, description, image }: FeatureProps) => (
          <Card key={title}>
            <CardHeader>
              <CardTitle>{title}</CardTitle>
            </CardHeader>

            <CardContent>{description}</CardContent>

            <CardFooter>
              <img
                src={image}
                alt={title}
                className="w-[200px] lg:w-[300px] mx-auto"
              />
            </CardFooter>
          </Card>
        ))}
      </div>
    </section>
  );
};
